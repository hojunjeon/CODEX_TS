[project]
name = "efts"
version = "0.1.0"
description = "EFTS: Codex-only token saver for Windows Codex Desktop and CLI"
requires-python = ">=3.10"
readme = "README.md"
license = { text = "MIT" }

[project.scripts]
efts = "efts.__main__:main"

[tool.setuptools]
packages = ["efts"]
